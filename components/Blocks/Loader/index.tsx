import React from 'react'
import StyledLoaderWrapper from './styled'

type Props = {}

function index({ }: Props) {
    return (
        <StyledLoaderWrapper>
            Loading
        </StyledLoaderWrapper>
    )
}

export default index